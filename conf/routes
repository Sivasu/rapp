# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /reviews_by_month           controllers.ReviewController.reviewsByMonth()
GET     /contributors_by_month           controllers.ReviewController.contributorsByMonth()
GET     /reviews_by_month_json      controllers.ReviewController.reviewsByMonthJson()
GET     /contributors_by_month_json      controllers.ReviewController.contributorsByMonthJson()
GET     /last_month_contributor      controllers.ReviewController.lastMonthContributorJson()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
